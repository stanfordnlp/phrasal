#!/bin/bash

#############################################################
# Extract 1best from an xml nbest list (ibm format)
#############################################################

grep '^<hyp r="0"' | perl -pe 's/<sco>.*//; s/<.*?>//g; s/ +/ /g; s/^ //; s/ $//' 
