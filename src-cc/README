To update kenlm, clone the repository:

  git clone https://github.com/kpu/kenlm.git

Then rsync the directories:

  rsync -rtvu --delete $KEN_LM_REPO kenlm/

You might want to try a dry-run first. Add the "-n" option to the rsync command above.

Spence
23 September 2015

