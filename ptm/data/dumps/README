To create the database dump, make sure that the .pgpass file is correct. Then run:

pg_dump -h localhost -U django djangodb > djangodb.dump

For more info on dump and restore, see:

  http://www.postgresql.org/docs/9.1/static/backup.html

Spence
20 March 2012
